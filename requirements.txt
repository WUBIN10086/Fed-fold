# 由 environment.yml 导出，Python 依赖部分
#
# === 推荐一次性安装顺序（新环境） ===
# 0. 系统：安装 CUDA Toolkit 并设置 CUDA_HOME（仅首次）
# 1. pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
# 2. pip install -r requirements.txt
# 3. 在项目根目录：pip install . --no-build-isolation   # 编译并安装 openfold + attn_core_inplace_cuda
# 4. 可选：pip install flash-attn --no-build-isolation

setuptools==59.5.0
pytorch-lightning
biopython
numpy
pandas
PyYAML
requests
scipy
tqdm
typing-extensions
wandb
modelcif==0.7
ml-collections
pdbfixer
openmm

# 原 environment.yml 的 pip 部分
deepspeed==0.14.5
dm-tree==0.1.6
git+https://github.com/NVIDIA/dllogger.git
# flash-attn 需在安装 torch 后单独装: pip install flash-attn --no-build-isolation
